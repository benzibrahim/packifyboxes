import {notFound} from 'next/navigation';
import {guides,origin} from '@/lib/content';
import {CTA} from '@/components/site-shell';
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const g=guides.find(g=>g.slug===slug);if(!g)return{};return{title:g.title+' | PackifyBoxes',description:g.intro,alternates:{canonical:origin+'/guides/'+slug},openGraph:{title:g.title,description:g.intro,url:origin+'/guides/'+slug,images:[]},twitter:{card:'summary',title:g.title,description:g.intro,images:[]}}}
export default async function Guide({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const g=guides.find(g=>g.slug===slug);if(!g)notFound();return <main id="main"><article className="wrap article"><div className="breadcrumb"><a href="/guides">Packaging guides</a><span>/</span>{g.category}</div><div className="eyebrow">{g.category}</div><h1>{g.title}</h1><p className="lead">{g.intro}</p>{g.sections.map(([t,d],i)=><section key={t}><span className="eyebrow">0{i+1}</span><h2>{t}</h2><p>{d}</p></section>)}<a className="text-link" href="/quote">Put your idea into a packaging brief →</a></article><CTA/></main>}
